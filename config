# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# Variables
set $primary HDMI-0
set $secondary eDP-1-1

set $theme wave
set $mode dark

# Scripts
exec --no-startup-id $HOME/.config/i3/scripts/display-layout
exec --no-startup-id $HOME/.config/i3/scripts/autostart.sh
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id $HOME/.fehbg
exec_always --no-startup-id $HOME/.local/bin/legion-kb-rgb load-profile -p $HOME/.config/keyboard-rgb/wave.json

# Files (Order is important)
include themes/$theme/$mode.conf
include workspaces.conf
include keybinds.conf
include rules.conf

# Font
font pango:JetBrainsMono Nerd Font 10

# Fallback font
font pango:DejaVu Sans Mono 10

# Mod key
set $mod Mod4
floating_modifier $mod
tiling_drag modifier titlebar

